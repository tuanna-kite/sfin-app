import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileVerification = () => {
  return (
    <View>
      <Text>ProfileVerification</Text>
    </View>
  );
};

export default ProfileVerification;

const styles = StyleSheet.create({});
